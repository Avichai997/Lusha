version: '3.8'
services:
  server:
    build:
      context: ./scraping-system
    ports:
      - 3000:3000
    depends_on:
      - mongo

  mongo:
    image: mongo:latest
    ports:
      - 27018:27017
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
