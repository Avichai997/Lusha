{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "scraping-system_service",
      "type": "node",
      "request": "attach",
      "port": 9234,
      "restart": true,
      // Allow only one of the following:
      // "localRoot": "${workspaceFolder}/scraping-system", // Allow this for LOCAL debugging
      "remoteRoot": "/app" // Allow this for DOCKER debugging
    }
  ]
}
